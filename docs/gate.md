# gate.md — 今どこで止まっているか

AI も人間も、作業前にこのファイルを見る。

| 項目 | 値 |
| --- | --- |
| ループ | L12 社会ドリル |
| ゲート | **B 承認済み** |
| 状態 | `stopped` |
| コードを書いてよいか | **いいえ** |
| ブランチ | `loop/l12-shakai-drill` |

## L12 承認内容

- しゃかいドリル 4種類（とどうふけん・にほんのちり・ちずきごう・けんのかたち）
- 国語ドリルの文脈例文改善（承認時点の品質。追加の指摘はテストしながら対応）

## 次の人間アクション

1. 必要なら `マージして` / PR 作成を指示
2. 国語の不自然な例文は都度フィードバック → 小さな修正 or 次ループで対応
3. 次の機能ループへ進むときは L13 提案から（ゲート A）

**注意**: スキーマ変更あり。既存 DB 利用時は `backend/data/minchalle.db` を削除して API 再起動。

## 状態の意味

| 状態 | AI | 人間 |
| --- | --- | --- |
| `waiting-A` | 提案まで。実装禁止 | 範囲を GO / 差し戻し |
| `implementing` | 承認済み範囲のみ実装 | 待ってよい |
| `waiting-B` | 結果提示まで。次ループ禁止 | 受け入れ / 差し戻し |
| `stopped` | 何もしない | 再開指示を出す |

## 履歴

- 2026-08-29: L12 ゲート B 承認。状態 `stopped`。
- 2026-08-29: L12 実装完了。ゲート B へ。
- 2026-08-29: 人間が「地図記号」「地図から県名」も入れたいと指示。L12 提案を4種類に更新。
- 2026-08-29: L11 を main へマージ（PR #10）。状態 `stopped`。
- 2026-08-29: L10 を main へマージ（PR #9）。状態 `stopped`。
- 2026-08-28: 自律実装を止めて承認ゲートを設置。
