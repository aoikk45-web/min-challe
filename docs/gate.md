# gate.md — 今どこで止まっているか

AI も人間も、作業前にこのファイルを見る。

| 項目 | 値 |
| --- | --- |
| ループ | L13 国語読解 |
| ゲート | **B 承認済み（PR 待ち）** |
| 状態 | `stopped` |
| コードを書いてよいか | **いいえ（次ループ禁止）** |
| ブランチ | `loop/l13-dokkai` |

## L13 の結果（承認済み）

- 新種別 `おはなしのどくかい`（1話＋3問・4択・解説付き）
- バンク 90話（NotebookLM 原稿 → `dokkai_stories.json`、ステージ1〜6）
- ふりがな実行時付与（`kokugo_kanji.py`）、選択肢シャッフル、採点修正
- 県の形: 地域ズーム地図・関東離島除外・九州/沖縄の表示切替
- ホーム: 成長する星・ドリルカード文言更新
- ドキュメント: `docs/dokkai.md`、`.cursor/rules/dokkai.mdc`
- `pytest` 72件成功

## 次の人間アクション

- PR を確認して **マージ**（または差し戻し）
- マージ後: 既存 DB 利用時は `backend/data/minchalle.db` を削除して API 再起動

## 状態の意味

| 状態 | AI | 人間 |
| --- | --- | --- |
| `waiting-A` | 提案まで。実装禁止 | 範囲を GO / 差し戻し |
| `implementing` | 承認済み範囲のみ実装 | 待ってよい |
| `waiting-B` | 結果提示まで。次ループ禁止 | 受け入れ / 差し戻し |
| `stopped` | 何もしない | 再開指示を出す |

## 履歴

- 2026-08-30: L13 ゲート B 承認。状態 `stopped`（PR 待ち）。
- 2026-08-29: ゲート外の品質・バグ修正を main へマージ（PR #12）。状態 `stopped`。
- 2026-08-29: L12 を main へマージ（PR #11）。状態 `stopped`。
- 2026-08-28: 自律実装を止めて承認ゲートを設置。
