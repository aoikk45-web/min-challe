# gate.md — 今どこで止まっているか

AI も人間も、作業前にこのファイルを見る。

| 項目 | 値 |
| --- | --- |
| ループ | **L14 — 理科ドリル** |
| ゲート | **B 待ち** |
| 状態 | `waiting-B` |
| コードを書いてよいか | **いいえ**（差し戻し対応のみ） |
| ブランチ | `loop/l14-rika` |

## 直近

- L14 理科ドリル実装完了。ゲート B 待ち。
- README 整備を main へ push（`7437bbc`）。

**注意**: L14 で `drill_progress` に理科2種が追加。既存 DB 利用時は `backend/data/minchalle.db` を削除して API 再起動。

## 次の人間アクション

- 動作確認のうえ **ゲート B で承認**（`プッシュして` / `差し戻し: …`）


## 状態の意味

| 状態 | AI | 人間 |
| --- | --- | --- |
| `waiting-A` | 提案まで。実装禁止 | 範囲を GO / 差し戻し |
| `implementing` | 承認済み範囲のみ実装 | 待ってよい |
| `waiting-B` | 結果提示まで。次ループ禁止 | 受け入れ / 差し戻し |
| `stopped` | 何もしない | 再開指示を出す |

## 履歴

- 2026-08-30: L14 理科ドリル実装完了。状態 `waiting-B`。
- 2026-08-30: L14 ゲート A 承認（学年成長型ステージ）。
- 2026-08-30: L13 を main へマージ（PR #13）。状態 `stopped`。
- 2026-08-30: L13 ゲート B 承認。
- 2026-08-29: ゲート外の品質・バグ修正を main へマージ（PR #12）。状態 `stopped`。
- 2026-08-29: L12 を main へマージ（PR #11）。状態 `stopped`。
- 2026-08-28: 自律実装を止めて承認ゲートを設置。
