# gate.md — 今どこで止まっているか

AI も人間も、作業前にこのファイルを見る。

| 項目 | 値 |
| --- | --- |
| ループ | —（次ループ未定） |
| ゲート | **停止** |
| 状態 | `stopped` |
| コードを書いてよいか | **いいえ** |
| ブランチ | `main`（L11 マージ済み） |

## 直近の完了

L11 国語レベル＋文章題を main へマージ（PR #10, 2026-08-29）。

- かんじ・じゅくご: レベル1〜100、レベル40以降は最後2問が文章題
- 語彙バンク: 漢字1,026字 + 熟語356語
- 広辞苑完全網羅は X5（後回し）

## 次に人間がすること

次のループの指示を出す。

## 状態の意味

| 状態 | AI | 人間 |
| --- | --- | --- |
| `waiting-A` | 提案まで。実装禁止 | 範囲を GO / 差し戻し |
| `implementing` | 承認済み範囲のみ実装 | 待ってよい |
| `waiting-B` | 結果提示まで。次ループ禁止 | 受け入れ / 差し戻し |
| `stopped` | 何もしない | 再開指示を出す |

## 履歴

- 2026-08-29: L11 を main へマージ（PR #10）。状態 `stopped`。
- 2026-08-29: 人間が L11 を受け入れ（OK）。PR 作成。
- 2026-08-29: 人間が L11 を承認。実装開始。
- 2026-08-29: L10 を main へマージ（PR #9）。状態 `stopped`。
- 2026-08-29: L9 を main へマージ（PR #8）。状態 `stopped`。
- 2026-08-28: 自律実装を止めて承認ゲートを設置。
