# gate.md — 今どこで止まっているか

AI も人間も、作業前にこのファイルを見る。

| 項目 | 値 |
| --- | --- |
| ループ | L12 完了 |
| ゲート | — |
| 状態 | `stopped` |
| コードを書いてよいか | **いいえ** |
| ブランチ | `main` |

## 次の人間アクション

- 次ループ（L13）を始めるときは提案から（ゲート A）
- 国語の不自然な例文は都度フィードバック → 小さな修正 or 次ループで対応
- ゲート外の品質・バグ修正は PR #12 で main にマージ済み

**注意**: L12 でスキーマ変更あり。既存 DB 利用時は `backend/data/minchalle.db` を削除して API 再起動。

## 状態の意味

| 状態 | AI | 人間 |
| --- | --- | --- |
| `waiting-A` | 提案まで。実装禁止 | 範囲を GO / 差し戻し |
| `implementing` | 承認済み範囲のみ実装 | 待ってよい |
| `waiting-B` | 結果提示まで。次ループ禁止 | 受け入れ / 差し戻し |
| `stopped` | 何もしない | 再開指示を出す |

## 履歴

- 2026-08-29: ゲート外の品質・バグ修正を main へマージ（PR #12）。状態 `stopped`。
- 2026-08-29: ゲート外の品質・バグ修正を人間が承認。
- 2026-08-29: L12 を main へマージ（PR #11）。状態 `stopped`。
- 2026-08-29: L12 ゲート B 承認。
- 2026-08-29: L12 実装完了。ゲート B へ。
- 2026-08-29: L11 を main へマージ（PR #10）。状態 `stopped`。
- 2026-08-29: L10 を main へマージ（PR #9）。状態 `stopped`。
- 2026-08-28: 自律実装を止めて承認ゲートを設置。
