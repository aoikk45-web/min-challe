# gate.md — 今どこで止まっているか

AI も人間も、作業前にこのファイルを見る。

| 項目 | 値 |
| --- | --- |
| ループ | L11 国語レベル＋文章題 |
| ゲート | **B 承認済み（PR 待ち）** |
| 状態 | `waiting-merge` |
| コードを書いてよいか | **いいえ** |
| ブランチ | `loop/l11-kokugo-level`（push 済み・PR 待ち） |

## L11 の実装結果

国語ドリル2種（**かんじのよみ** / **じゅくごのよみ**）を算数と同様に **レベル1〜100** で進めるようにした。レベル40以降は最後2問が **文章題**。

- **語彙バンク**: 学習漢字 **1,026字** ＋ 熟語 **356語**（学年別 JSON）
- **レベルアップ**: 10問全問正解 × 5回連続（算数と同じ）
- **表示**: こくごボタン・保護者画面にもひよこ→鶏・★
- **広辞苑完全網羅**: 未実装（X5）。カリキュラム準拠バンクまで

### 検証

- `backend`: pytest **44 passed**
- `frontend`: `npm run build` 成功

### 動作確認の注意

既存 DB には国語の `drill_progress` 行がない場合あり。**API 停止後に DB 削除＋再起動**でシードし直す。

## 次に承認してほしいこと

PR を確認して **マージして** と指示。

## 履歴

- 2026-08-29: 人間が L11 を受け入れ（OK）。PR 作成。

| 状態 | AI | 人間 |
| --- | --- | --- |
| `waiting-A` | 提案まで。実装禁止 | 範囲を GO / 差し戻し |
| `implementing` | 承認済み範囲のみ実装 | 待ってよい |
| `waiting-B` | 結果提示まで。次ループ禁止 | 受け入れ / 差し戻し |
| `stopped` | 何もしない | 再開指示を出す |

## 履歴

- 2026-08-29: L11 実装完了。ゲートB 待ち。ブランチ `loop/l11-kokugo-level`。
- 2026-08-29: 人間が L11 を承認。実装開始。
- 2026-08-29: 人間が「国語に文章題」「レベル1-100」「広辞苑網羅」を指示。L11 ゲートA 提案。
- 2026-08-29: L10 を main へマージ（PR #9）。状態 `stopped`。
