# misutes.md — 議事録・判断・学び

ファイル名は指定どおり `misutes.md`（minutes と、ミス／テストの記録）。

新しい記事を上に足す。

---

## 2026-08-29 L10 マージ（stopped）

- 人間が「マージして」指示。PR #9 を main へマージ。
- 算数ドリル段階レベル（ステップ1〜100、★5つでレベルアップ、ひよこ→鶏UI）が本番ブランチに入った。

---

## 2026-08-29 L10 ゲートB 承認（PR 待ち）

- 人間が L10 を承認。表示はレベル1〜100・ひよこ→鶏アイコン・★のみ（バーと「ぜんぶ正解」テキストは削除）。
- pytest 43、build OK。PR 作成。

---

- 人間が L10 を承認。ブランチ `loop/l10-drill-steps`。
- **バックエンド**: `drill_progress` テーブル、`drill_sessions.step`、`drill_progress.py`（ストリーク・ステップアップ）、`generate.py` をステップ1〜100対応に書き換え。`GET /api/drills/progress` 追加。
- **フロント**: 種類選択・出題中・結果・保護者履歴にステップメーター。ステップアップ時に「ステップアップ！」。
- **テスト**: pytest 43件（L10 で +5）。`npm run build` OK。
- **既存DB**: `minchalle.db` 削除＋API再起動でマイグレーション相当（シードで step1 から）。

---

## 2026-08-29 L10 提案の更新2（ゲートA 待ち）

- 人間: 100段階に。10問すべて正解が5回続いたらレベルアップ。
- **ステップ1〜100**（小1〜小6を約17段階ずつ）。全問正解でストリーク+1、1問でもミスで0、**5連続でステップ+1**。
- 表示: ステップ番号、バー（/100）、`ぜんぶ正解 3/5回`。
- 文章題はステップ40以降。
- コードは書いていない。

---

## 2026-08-29 L10 提案の更新（ゲートA 待ち）

- 人間: レベルアップの段階をもっと細かく。
- 6段階ではなく **ステップ1〜30**（各学年を約5段階に分割）。8問以上正解で **1ステップアップ**（連続2回は不要）。
- 表示: `ステップ 3` + バー（3/30）+ `小1のうち` などの短いラベル。
- 文章題はステップ12以降から（最初は計算式10問）。

---

## 2026-08-29 段階レベル → L10 提案（ゲートA 待ち）

- 人間: 初めは小1レベル、公文式のように繰り返して徐々にレベルアップ、最終小6。いまのれべル表示。
- L10 に入れる案: 算数4種ごとにれべル1〜6、連続2回8問以上でアップ、画面に表示。出題は既存の学年ルールを流用。
- L10 に入れない案: 国語の段階レベル、手動変更、弱点分析、レベルダウン。
- コードは書いていない。

---

## 2026-08-29 L9 マージ（stopped）

- 人間: 「マージしてください」→ PR #8 を `main` へマージ。B9 を done。
- 算数4種の 9・10問目が文章題になった。次ループのコードも提案もしていない。
- 後回しのまま: 弱点分析、写真、複数子ども、漢字の書き取り。人間が明示したときだけ起こす。

---

## 2026-08-29 L9 プッシュ（マージ待ち）

- 人間: 「プッシュして」→ ゲートB 承認として `loop/l9-word-problems` を push / PR。
- マージはしない。次は人間の「マージして」。

---

## 2026-08-29 L9 結果（ゲートB 待ち）

- 人間: 「L9 承認」→ ブランチ `loop/l9-word-problems`。
- 算数4種の 9・10問目を文章題にした。1〜8問目は計算式。こたえは数字のまま。
- 文章題は画面で少し小さくした。prompt 列を String(200) に広げた。
- 確認: `pytest` 38 passed。`npm run build` 成功。API で 1〜8 が式、9・10 が文章題になることを確認。

次: 受け入れたら push + PR。確認後「マージして」。

---

## 2026-08-29 文章題 → L9 提案（ゲートA 待ち）

- 人間: 算数ドリル4種類について、10問のうち最後の2問を文章題に。
- L9 に入れる案: たしざん・ひきざん・かけざん・わりざんの 9・10問目だけ文章題。数字範囲とこたえ（数字）は今と同じ。
- L9 に入れない案: 国語、2ステップ、あまり、新種類、弱点分析。
- コードは書いていない。

---

## 2026-08-29 L8 マージ（stopped）

- 人間: 「マージして」→ PR #7 を `main` へマージ。B8 を done。
- かんじ・じゅくごのよみまで揃った。次ループのコードも提案もしていない。
- 後回しのまま: 弱点分析、写真、複数子ども、漢字の書き取り（次なら4択）。人間が明示したときだけ起こす。

---

## 2026-08-29 L8 プッシュ（マージ待ち）

- 人間: 「プッシュして」→ ゲートB 承認として `loop/l8-kokugo` を push / PR。
- マージはしない。次は人間の「マージして」。

---

## 2026-08-29 L8 結果（ゲートB 待ち）

- 人間: 「L8 承認」→ ブランチ `loop/l8-kokugo`。
- ドリルに `かんじのよみ`（一字）と `じゅくごのよみ` を足した。3年バンクから 10問。計算の4種はそのまま。
- こたえは文字列。カタカナ・空白はひらがなにそろえて採点する。書き取りはやっていない。
- 解答列を文字列にしたので、デモ DB `backend/data/minchalle.db` は消して再起動する。
- 確認: `pytest` 37 passed。`npm run build` 成功。UI でこくご 2ボタンとひらがな入力を確認。

次: 受け入れたら push + PR。確認後「マージして」。

---

## 2026-08-29 L8 提案の更新（ゲートA 待ち）

- 人間: 書き取りの難しさ、熟語のよみを入れられるか。
- 書き取り（手書き・IME）はこのループではやらない。次なら4択が低い。
- 熟語のよみは同じ仕組みなので L8 に入れる。

---

## 2026-08-29 国語ドリル → L8 提案（ゲートA 待ち）

- 人間: 「国語ドリルの追加」。X4 を起こして L8 として範囲提案。コードは書いていない。
- L8 に入れる案: 3年生の漢字の読み 10問、計算と同じポイント・アルバム。
- L8 に入れない案: 書き取り、読解、他学年、英語、弱点分析。

---

## 2026-08-29 L7 マージ → MVP 完了（stopped）

- 人間: 「マージして」→ PR #6 を `main` へマージ。B7 を done。
- MVP（L0〜L7）は揃った。次ループのコードも提案もしていない。
- 後回しのまま: 弱点分析、写真、複数子ども、他教科。人間が明示したときだけ起こす。

---

## 2026-08-29 L7 プッシュ（マージ待ち）

- 人間: 「pushしてください」→ ゲートB 承認として `loop/l7-polish` を push / PR。
- マージはしない。次は人間の「マージして」。

---

## 2026-08-29 L7 結果（ゲートB 待ち）

- 人間: 「L7 承認」→ ブランチ `loop/l7-polish`。
- README を4本柱と起動手順（Python 3.12、Windows は `py -3.12`、`--reload` なし）に合わせた。
- ホームの空状態をロールで分けた。ポイントの読み込み中を出した。ドリル保護者の空コピーを揃えた。
- 4本柱 API が 200 になるテストを足した。新機能は足していない。
- 確認: `pytest` 34 passed。UI `http://127.0.0.1:48221` が 200。`npm run build` 成功。

次: 受け入れたら push + PR。確認後「マージして」。

---

## 2026-08-29 L6 マージ → L7 提案（ゲートA 待ち）

- 人間: 「マージして」→ PR #5 を `main` へマージ。B6 を done。
- 次は L7 統合・README の範囲承認。コードは書いていない。
- L7 に入れる案: README を今の機能と起動手順に合わせる、空状態コピーの揃え。
- L7 に入れない案: 新機能、写真、弱点分析、認証。

---

## 2026-08-29 L6 プッシュ（マージ待ち）

- 人間: 「プッシュして」→ ゲートB 承認として `loop/l6-custom-awards` を push / PR。
- マージはしない。次は人間の「マージして」。

---

## 2026-08-29 L6 結果（ゲートB 待ち）

- 人間: 「L6 承認」→ ブランチ `loop/l6-custom-awards`。
- カスタムルールを画面からけせる（ほぞん後）。自動ルールは消せない。
- スタンプで付与ルールを選べる。デモに「テスト100点」20pt。
- 計画・ドリルの自動キーでは押せない。子どもは 403。
- 確認: `pytest` 33 passed。フロント `npm run build` 成功。

次: 受け入れたら push + PR。確認後「マージして」。

---

## 2026-08-29 L5 マージ → L6 提案（ゲートA 待ち）

- 人間: 「マージしてください」→ PR #4 を `main` へマージ。B5 を done。
- 次は L6 ルール削除と学校のごほうび。コードは書いていない。
- L6 に入れる案: カスタムルールの削除、スタンプで付与ルールを選ぶ、デモにテスト100点。
- L6 に入れない案: 自動ルールの削除、子どもによる付与、学校連携、README。

---

## 2026-08-29 L5 プッシュ（マージ待ち）

- 人間: 「L5を先に出してください」→ ゲートB 承認として `loop/l5-album` を push / PR。
- ルール削除と学校のごほうび（テスト100点など）は今の PR に入れない。マージ後の次ループで提案する。
- マージはしない。次は人間の「マージして」。

---

## 2026-08-29 L5 結果（ゲートB 待ち）

- 人間: 「L5 承認」→ ブランチ `loop/l5-album`。
- テーブル `album_entries`。自動記録: 計画完了 / ドリル完了 / ごほうび交換 / できたねスタンプ。
- 保護者メモ POST。子どもは 403。新しい順。種類は絵文字（📒✨🎁🏅📝）。
- 0 件なら「最初のページは、これからつくられるよ」。デモシードで数件入っている。
- 写真・削除・編集は未実装。
- 確認: `pytest` 29 passed。GET `/api/album` 200。UI `http://127.0.0.1:48221` が 200。フロント `npm run build` 成功。

次: 受け入れたら push + PR。確認後「マージして」。

---

## 2026-08-29 L4 マージ → L5 提案（ゲートA 待ち）

- 人間: 「マージして」→ PR #3 を `main` へマージ。B4 を done。
- 次は L5 成長アルバムの範囲承認。コードは書いていない。
- L5 に入れる案: 自動記録（計画・ドリル・交換・スタンプ）、保護者メモ、タイムライン。
- L5 に入れない案: 写真、削除・編集、SNS。

---

## 2026-08-29 L4 プッシュ（マージ待ち）

- 人間: 「pushして」→ ゲートB 承認として `loop/l4-points` を push / PR。
- マージはしない。次は人間の「マージして」。

---

## 2026-08-28 L4 結果（ゲートB 待ち）

- 人間: 「L4 承認」→ ブランチ `loop/l4-points`。
- ルール・ごほうび・台帳。計画完了 8pt、ドリル完了 10pt、全問正解 +5、スタンプ 3pt。
- 交換は即時減点。不足は 400「あと○点」。子どもはルール編集 403。
- アルバムは未実装。
- 確認: `pytest` 23 passed。summary 200、child PUT 403。UI 200。

次: 受け入れたら push + PR。確認後「マージして」。

---

## 2026-08-28 L3 マージ → L4 提案（ゲートA 待ち）

- 人間: 「マージして」→ PR #2 を `main` へマージ。B3 を done。
- 次は L4 ポイント制の範囲承認。コードは書いていない。
- L4 に入れる案: ルール・ごほうび・スタンプ・残高・交換、計画/ドリル完了時の付与。
- L4 に入れない案: アルバム、交換の再承認。

---

## 2026-08-28 L3 プッシュ（マージ待ち）

- 人間: 「プッシュして」→ ゲートB 承認として `loop/l3-drill` を push / PR。
- マージはしない。次は人間の「マージして」。

---

## 2026-08-28 L3 結果（ゲートB 待ち）

- 人間: 「L3 承認」→ ブランチ `loop/l3-drill`。
- 10問生成（3年生範囲）。わりざんは割り切れる。未解答の正解は API に出さない。
- 子どもが 1 問ずつ解答。保護者の start は 403。履歴は両方。
- ポイント・アルバムは未実装のまま。
- 確認: `pytest` 17 passed。HTTP で start 200 / parent 403。UI `http://127.0.0.1:48221` が 200。

次: 受け入れたら push + PR。確認後「マージして」。

---

## 2026-08-28 L2 マージ → L3 提案（ゲートA 待ち）

- 人間: 「マージして」→ PR #1 を `main` へマージ。B2 を done。
- 次は L3 計算ドリルの範囲承認。コードは書いていない。
- L3 に入れる案: 10問の出題・その場採点・結果（時間・正解数）・履歴。学年は 3 年。
- L3 に入れない案: ポイント、アルバム、弱点分析、保護者の出題開始。

---

## 2026-08-28 L2 プッシュ（マージ待ち）

- 人間: 「プッシュして」→ ゲートB 承認として `loop/l2-study-plan` を push / PR。
- マージはしない。次は人間の「マージして」。

---

## 2026-08-28 L2 結果（ゲートB 待ち）

- 人間: 「L2 承認。minsuke と同様、ループごとにブランチ → 承認後 push → 確認後マージ」
- ブランチ `loop/l2-study-plan`（未 push）。`loop.md` に Git 運用を書いた。
- `study_plans` と API（一覧 / 作成 / 更新 / 削除 / 完了）。子どもは作成・更新・削除 403。
- デモ今週シード。過去日済み、今日以降未完了。
- `/plan` とホームのきょうのけいかく。ポイント・アルバムは未実装のまま。
- 確認: `pytest` 10 passed。API の完了・作成・更新・削除を HTTP で確認。UI は `http://127.0.0.1:48221` が 200。ブラウザ自動操作はこの環境では使えなかった。

次: 人間が受け入れたら push + PR。確認後「マージして」で main。

---

## 2026-08-28 L1 受け入れ → L2 提案（ゲートA 待ち）

- 人間: 「開発のループの続きを」→ L1 ゲートB を通したと解釈。
- B1 を done。次は L2 学習計画の範囲承認。
- L2 に入れる案: 計画 CRUD、今週一覧、できた、デモシード、ホームの今日カード。
- L2 に入れない案: ポイント付与、アルバム記録（仕様では完了時に付くが、ループ分割どおり L4/L5）。

コードは書いていない。人間の `L2 承認` / `GO` / 差し戻しを待つ。

---

## 2026-08-28 L1 結果（ゲートB 待ち）

- デモ学年を 3 に変更してシード。
- FastAPI `GET /api/household` がさくら家・みんすけ3年生・おかあさんを返す。
- React: ロール切替、ホーム、4本柱ナビ、空状態。
- 確認: `pytest` 3 passed。UI `http://127.0.0.1:48221` が 200。
- やらなかった: 計画CRUD、ドリル、ポイント計算、アルバム記録。

次は人間が L1 を受け入れたら、L2 学習計画の提案。

---

## 2026-08-28 仕様承認 → L1 開始

- 人間: 「デモはさくら家 / みんすけ 3年生」「ひとまずこの仕様ですすめて」
- `spec.md` を承認済みに変更。学年を 3 に修正。
- L0 完了。L1（土台・デモ家庭・4本柱ナビ）だけ実装する。
- 計画・ドリル・ポイント計算・アルバム記録は L2 以降。

---

## 2026-08-28 大まかな仕様を草案（コードなし）

人間から「まず大まかな仕様を」と指示。実装はしていない。

- 正本候補: `docs/spec.md`
- いまのゲート: L0 ゲートA（仕様の承認待ち）
- 承認してほしい点: 4本柱の範囲、家庭設定のポイント、1家庭デモ、仮名「みんチャレ」
- 承認後の次: 実装ではなく L1 土台の設計提案 → また人間 GO 待ち

---

## 2026-08-28 自律実装を止めた（プロセス修正）

### ミス

- キックオフで `roles.md` 等を置いた直後、承認を待たずに FastAPI 実装を始めた。
- `roles.md` に「止まって聞かない。Cloud Agent は自律する」と書いてしまい、`minsuke-springboot_02` の型と逆だった。

### 是正

- 書きかけの `backend/` を削除した。
- 人間承認ゲートを `docs/gate.md` / `docs/loop.md` / `docs/roles.md` に明文化した。
- いまの状態は **L0 ゲートA 待ち**。コード禁止。

### 確認した進め方（人間の指摘どおり）

1. AI はループの提案（要件・設計・完了条件）を出す。
2. 人間が承認するまで実装しない。
3. 承認された 1 ループだけ実装・テストし、結果を出す。
4. 人間が受け入れてから、次ループの提案に入る。

### 次に人間へ

- L0（進め方と MVP 範囲）を承認するか、直しの指示を出す。
- 承認後の最初の実装ループ案は L1（空の FastAPI + React とデモ家庭）。

---

## 2026-08-28 キックオフ（旧。一部取り消し）

### 当時の決定（プロセス以外は草案として残す）

- プロダクト名案: **みんチャレ**
- スタック案: FastAPI + React（Vite）、SQLite、認証なしのロール切替
- 4本柱以外は later

### 取り消し

- 「仮定を書いて自律実装する」は取り消し。承認待ちに戻した。
